# Rendered by the Ansible playbook into {{ app_dir }}/infra/.env (mode 0600). Do not edit by
# hand on the host — re-run the playbook. Values come from group_vars (ansible-vault) or the
# release workflow's --extra-vars.
GHCR_OWNER={{ ghcr_owner }}
COMPANYOPS_VERSION={{ companyops_version }}
APP_DOMAIN={{ app_domain }}
KEYCLOAK_DOMAIN={{ keycloak_domain }}
ACME_EMAIL={{ acme_email }}
POSTGRES_PASSWORD={{ postgres_password }}
KC_DB_PASSWORD={{ kc_db_password }}
RABBITMQ_PASSWORD={{ rabbitmq_password }}
KC_ADMIN_USER={{ kc_admin_user }}
KC_ADMIN_PASSWORD={{ kc_admin_password }}
OTEL_EXPORTER_OTLP_ENDPOINT={{ otel_exporter_otlp_endpoint | default('') }}
