[defaults]
inventory = inventory.ini
# host_key_checking stays at its secure default (true). On first connect, accept/verify the
# VM's host key (e.g. `ssh-keyscan <ip> >> ~/.ssh/known_hosts`) — see docs/deployment.md.

[ssh_connection]
pipelining = true
